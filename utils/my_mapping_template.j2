<!-- Source = {in_path}. This file is generated by USCDI-USCore_mappingpage_maker.ipynb Do not edit directly. -->


<style>

.panel {
    background: #0074bb;
    color: white;
    font-weight: bold;
    margin-bottom: 3px;
    margin-right: 40px;
    margin-left: 40px;
    padding: 0px;
    box-shadow: 1px 2px DarkGray;
    }

.panel-body {
    padding-left: 15px;
}

.panel table {
    /* table-layout: fixed;
    word-wrap: break-word;
    width: auto; */
    border: 0px !important;
    padding-right: 20px;
    }

.panel th, td {
    font-weight: bold;
    text-align: left;
    }

 .panel a {
    color: white;
    font-weight: bold;
    text-decoration: underline;
    }

 .icon-right {
    float: right;
    padding: 6px
}

</style>

<div class="icon-right">
    <img src="USCDI.png" class="media-object" alt="USCDI Logo">
 </div>
<h3>
US Core Data for Interoperability USCD V3
</h3>

{% for df in dfs %}
{% for j,row in df.iterrows() %}
{% if loop.first %}
<div class="panel">
  <div class="panel-body">
    <div class="icon-right">
    <img src="{{row.data_class | lower | replace (" ", "-") | replace ("/", "-")}}.png" class="media-object" alt="{{row.data_class}}" style="width:60px">
  </div>
  <table class="table table-condensed">
    <thead>
      <tr>
        <th class="col-sm-4">{{row.data_class}}</th>
        <th class="col-sm-4">{{row.uscore_profile | replace("|", "<br />")| safe }}</th>
        <th class="col-sm-2"></th> 
      </tr>
    </thead>
    <tbody>
  {% else %}
      <tr>
        <td>&nbsp;&nbsp;&nbsp;&nbsp;&bull;{{row.data_element}}</td>
        <td>{{row.uscore_profile | replace("|", "<br />")| safe }}</td>
        <td>{{row.comment | safe }}</td>
      </tr>
  {% if loop.last %} 
    </tbody>
  </table>
  </div>
</div>
{% endif %}
{% endif %}
{% endfor %}
{% endfor %}


